import React from 'react';

import logo from './logo.svg';

import './header.scss';

const Header = () => (
    <div className="Header">
        <img src = {logo} />
    </div>
);

export default Header;